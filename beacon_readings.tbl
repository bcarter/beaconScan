--------------------------------------------------------
--  File created - Tuesday-September-12-2017
--------------------------------------------------------
WHENEVER SQLERROR EXIT
--------------------------------------------------------
--  DDL for Table READING
--------------------------------------------------------

CREATE TABLE "BEACON"."READING" (
    "ID"           NUMBER
        GENERATED BY DEFAULT ON NULL AS IDENTITY,
    "SCANNER_ID"   NUMBER,
    "BEACON_ID"    VARCHAR2(1000 BYTE),
    "DISTANCE"     NUMBER,
    "CREATED_ON"   TIMESTAMP(6) WITH LOCAL TIME ZONE DEFAULT localtimestamp
);

--------------------------------------------------------
--  DDL for Index READING_UK
--------------------------------------------------------

CREATE UNIQUE INDEX "BEACON"."READING_UK" ON
    "BEACON"."READING" ( "ID" );
  
--------------------------------------------------------
--  Constraints for Table READING
--------------------------------------------------------

ALTER TABLE "BEACON"."READING" MODIFY (
    "CREATED_ON"
        NOT NULL ENABLE
);

ALTER TABLE "BEACON"."READING" ADD CONSTRAINT "READING_PK" PRIMARY KEY ( "ID" )
    USING INDEX reading_uk
ENABLE VALIDATE;